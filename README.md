# Chrome Plugin for Keycloak Swagger Authentication

Chrome Plugin that calls the token endpoint of Keycloak to obtain a bearer token, that in turn can be used to 
authenticate API requests in Swagger UI.
